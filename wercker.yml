box: perl:5.18.2

build:
  steps:
    - script:
        name: install dependencies
        code: |
          carton install

  after-steps:
    - wantedly/pretty-slack-notify:
        webhook_url: $SLACK_WEBHOOK_URL
